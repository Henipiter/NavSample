package com.example.navsample.DTO

import android.graphics.Color

class ChartColors {


    companion object {
        val COLORS = intArrayOf(
            Color.rgb(255, 0, 0), // Czerwony
            Color.rgb(0, 255, 0), // Zielony
            Color.rgb(0, 0, 255), // Niebieski
            Color.rgb(128, 0, 128), // Purpurowy
            Color.rgb(0, 128, 128), // Turkusowy
            Color.rgb(255, 69, 0), // Czerwony pomarańczowy
            Color.rgb(0, 128, 0), // Zielony trawiasty
            Color.rgb(255, 99, 71), // Czerwony pomidorowy
            Color.rgb(30, 144, 255), // Błękitny
            Color.rgb(255, 215, 0), // Złoty
            Color.rgb(0, 255, 255), // Jasnoniebieski
            Color.rgb(218, 112, 214), // Orchidea
            Color.rgb(0, 255, 127), // Zielony szmaragdowy
            Color.rgb(255, 182, 193), // Różowy jasny
            Color.rgb(75, 0, 130), // Indygo
            Color.rgb(255, 140, 0) // Pomarańczowy ciemny
        )
    }
}
